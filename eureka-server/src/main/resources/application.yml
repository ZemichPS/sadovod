server:
  port: 8761

eureka:
  instance:
    hostname: eureka-server
  server:
    enable-self-preservation: true
  client:
    register-with-eureka: false
    fetch-registry: false
    healthcheck:
      enabled: true

spring:
  application:
    name: eureka-server